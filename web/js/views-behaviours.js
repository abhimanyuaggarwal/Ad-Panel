// views-behaviours.js — the player's one-line summary. The player is neither an identity
// (24 Aug) nor a card (25 Aug): after the trim it is three fields inside the
// integration's Identity — how it starts a video, and what it falls back to.

